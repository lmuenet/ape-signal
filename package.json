{
  "name": "ape-signal",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "VPS daily trading routines (pre-open scan + challenge, reddit crawl, telegram analyst) on top of ape-intel.",
  "scripts": {
    "scan": "tsx src/scan/index.ts",
    "listener": "tsx src/telegram/listener.ts",
    "doctor": "tsx src/config/doctor.ts",
    "gen-timers": "tsx src/config/genTimers.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "tick": "tsx src/paper/tick.ts",
    "ui": "tsx src/ui/main.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "lightweight-charts": "^4.2.3"
  }
}
